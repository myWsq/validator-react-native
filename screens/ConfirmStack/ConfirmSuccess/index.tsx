import * as React from 'react';
import Container from '../../../components/Container';

export interface ConfirmSuccessProps {
}

export interface ConfirmSuccessState {
}

export default class ConfirmSuccess extends React.Component<ConfirmSuccessProps, ConfirmSuccessState> {
  constructor(props: ConfirmSuccessProps) {
    super(props);

    this.state = {
    }
  }

  public render() {
    return (
      <Container>
        
      </Container>
    );
  }
}
